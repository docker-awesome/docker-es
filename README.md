# Docs

```bash
pnpm add [package]

# 参数:
# -D : 依赖安装到 devDependencies
# --workspace: Only adds the new dependency if it is found in the workspace.
# --filter [package]: 依赖安装到指定 package 下

# 示例:
# pnpm add @docker-es/hub --workspace --filter @docker-es/react
```
